{
  "name": "node-server",
  "version": "0.0.1",
  "description": "Node in a network of nodes",
  "main": "index.js",
  "scripts": {
    "discovery": "cross-env NODE_ENV=development nodemon node-discovery.js",
    "cs": "cross-env NODE_ENV=development nodemon node-server.js",
    "chatserver": "cross-env NODE_ENV=development node node-server.js",
    "test": "cross-env NODE_ENV=test echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "author": "dsatter group",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3",
    "express": "^4.18.2",
    "ws": "^8.10.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.26.0",
    "nodemon": "^2.0.20"
  }
}
